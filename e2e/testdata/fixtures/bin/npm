#!/bin/sh
# Fixture `npm` for the E2E suite.
if [ "$1" = "run" ]; then
  shift
  name="$1"
  shift
  case "$name" in
    debug|dev|build|start|lint|pre-build|post-build)
      echo "[fixture] npm run $name (ok)"
      exit 0
      ;;
    broken)
      echo "[fixture] npm run broken (ko)"
      exit 42
      ;;
    *)
      echo "[fixture] npm run $name (script introuvable)"
      exit 1
      ;;
  esac
fi
if [ "$1" = "install" ]; then
  echo "[fixture] npm install (ok)"
  exit 0
fi
echo "[fixture] npm $*"
exit 0