mkdir project
cd project
mkdir external_modules
exec sh -c 'printf "app = \"demo\"\n" > sentient.config.toml'
exec $SENTIENTS --no-color create module blog-manager --domain com.example.blog-manager --version 1.0.0
exec sh -c 'mkdir -p node_modules/@sentients/sdk node_modules/@tanstack/react-query node_modules/@tanstack/react-table node_modules/date-fns node_modules/lucide-react node_modules/next node_modules/react node_modules/react-dom node_modules/sonner'

# TC-017 — conforming module: audit succeeds, no errors.
exec $SENTIENTS --no-color audit com.example.blog-manager
stdout '✓ Audit passed — no errors, no warnings'
stdout 'valid UUID token'

# JSON output (spec §5.10): machine-usable form.
exec $SENTIENTS --no-color audit --output json
stdout '"modules"'
stdout '"findings"'

# TC-018 — broken module (no default export): audit errors.
exec sh -c 'printf "export const x = 1;\n" > external_modules/com.example.blog-manager/index.tsx'
exec $SENTIENTS --no-color audit com.example.blog-manager
stderr 'Summary: 2 error\(s\)'
stdout 'export not compliant'